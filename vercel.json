{
  "framework": null,
  "installCommand": "cd .. && npm install",
  "buildCommand": "cd .. && npm run build && node scripts/copy-web-dist.mjs",
  "outputDirectory": "../dist",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api" }
  ]
}
